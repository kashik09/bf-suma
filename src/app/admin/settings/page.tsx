import { Card, SectionHeader } from "@/components/ui";

export default function AdminSettingsPage() {
  return (
    <div className="space-y-6">
      <SectionHeader title="Settings" description="Business and delivery settings scaffold." />
      <Card>Settings form placeholder</Card>
    </div>
  );
}
